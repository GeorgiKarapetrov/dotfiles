import math

x = int ( input("Enter a number: ") )

if x < 2:
    print ( "The number", x, "is not prime." )
    


primes = [2,3]
booleanIndex = 1
for prime in primes
    booleanIndex =* prime
[divisor, newPrime]


def check(n,boolean,booleanIndex,divisor):
    
    while divisor < int( math.sqrt(n) ) + 1:
        if n % divisor == 0 || n % (divisor+2) == 0 || boolean:
            i += booleanIndex
            continue
        else
            return [divisor, n]
        
    return [divisor, newPrime]
    

if check(x):
    print ( "The number", x, "is prime." )
else:
    print ( "The number", x, "is not prime." )
let 