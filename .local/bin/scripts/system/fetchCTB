#!/bin/bash

set -x

mount | grep "${HOME}/Documents/cloud/gdrive" >/dev/null || google-drive-ocamlfuse -label home "${HOME}/Documents/cloud/gdrive"

rsync -acurPO -vv --info=stats2,misc1,flist0 --delete-after $HOME/Documents/cloud/gdrive/ctb $HOME/Documents/misc
